import AppContext from "AppContext";
import LanguageChoice from "pages/LanguageChoice";

function App() {
  return (
    <AppContext>
      <LanguageChoice />
    </AppContext>
  );
}

export default App;
